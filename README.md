mocklocationcheckbypasser
=========================

An Xposed module that intercepts calls to present `mock_location` setting as `'0'`. Serves as an illustration on how to build a minimal Xposed module.
